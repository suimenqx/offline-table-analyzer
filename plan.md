# Offline Table Analyzer 实施计划

## 范围与约束

- 继续保持单文件 `index.html`、完全离线、运行时零依赖和现有 25 MB 输入限制。
- 不引入 Web Worker、IndexedDB、虚拟滚动、流式导出或其他大数据能力。
- 保留 schema 20、现有解析器、JOIN 类型、复制格式、工作区备份和单元格修订兼容性。
- 每个完成项必须有代码、测试或静态检查证据；完成后立即将对应 `[ ]` 更新为 `[x]`。
- 所有开发直接在 `main` 分支进行，不创建主题分支，不创建 PR。

## 完成状态

### 0. 基线与执行规则

- [x] 0.1 确认 `main` 分支、工作区干净、现有单元/集成测试通过。
- [x] 0.2 建立本计划文件，并让构建产物由源代码重建后保持一致。

### 1. 状态生命周期统一

- [x] 1.1 增加统一的状态 revision、只读状态访问和命令结果约定。
- [x] 1.2 将 UI 对 Store 的直接写入迁移到 `dispatch` / `Store.transition`。
- [x] 1.3 增加统一状态变更事件和渲染调度，合并同一轮重复渲染。
- [x] 1.4 增加架构静态检查，禁止 UI 直接修改 Store 状态或直接保存。
- [x] 1.5 完成状态命令、事件和渲染次数测试。

### 2. 源版本与单元格修订隔离

- [x] 2.1 为文档增加 `sourceRevision`、解析版本和结果来源元数据。
- [x] 2.2 将源文本、格式、表头模式变化统一收敛为 source transition。
- [x] 2.3 源变化时自动清理 cell edits、undo/redo 和 HTML 粘贴元数据。
- [x] 2.4 为旧版修订数据增加安全读取策略，禁止静默错配。
- [x] 2.5 增加源变化、异步文件导入、跨页签和旧备份修订隔离测试。

### 3. 统一 QueryResult 管线

- [x] 3.1 增加纯查询服务，统一 JOIN、过滤、高亮、Focus、列投影和分页。
- [x] 3.2 让 Preview、Copy、Preview Export 使用同一查询结果契约。
- [x] 3.3 增加按文档/源版本/视图版本/查询版本的派生结果缓存。
- [x] 3.4 让 TableBuilder 只负责渲染已准备好的页面数据。
- [x] 3.5 增加预览与导出一致性、缓存失效和重复计算测试。

### 4. UI 可见性与无障碍

- [x] 4.1 增加当前数据状态、过滤状态、JOIN 状态和修订数量展示。
- [x] 4.2 增加可删除的活动过滤 Chips，并保证与查询状态同步。
- [x] 4.3 展示格式识别候选的理由、风险和确认状态。
- [x] 4.4 增强单元格修订反馈和 JOIN 预览反馈。
- [x] 4.5 实现通用 Dialog 焦点陷阱、Escape 恢复焦点和合理的 live region。
- [x] 4.6 增加键盘操作、Dialog 焦点和窄屏布局测试。

### 5. 版本、迁移、发布与测试治理

- [x] 5.1 统一 package、HTML、运行时和备份中的应用版本来源。
- [x] 5.2 增加显式 workspace schema migration 入口和旧数据兼容测试。
- [x] 5.3 让架构校验覆盖 manifest、版本、离线边界和生成产物。
- [x] 5.4 补齐关键用户流程的集成测试和发布验证。
- [x] 5.5 更新架构、需求、路线图和用户文档中的过期版本/模块数量描述。

## 最终验收

- [x] A. 全量测试、发布校验和架构静态检查全部通过。
- [x] B. `index.html` 可由 `src/` 和模板确定性重建。
- [x] C. UI 不再直接写 Store，源变化不会错配修订。
- [x] D. Preview、Copy、Export 使用统一 QueryResult。
- [x] E. 关键 Dialog 支持键盘闭环与焦点恢复。
- [x] F. 版本、schema、备份和文档信息一致。
- [x] G. 变更已提交到 `main` 并推送到配置的主远程。
